import 'dart:ui';

class Constants {
  static const Color scaffoldBackgroundColor = Color(0xFFfee6fb);
  static const Color chatBoxColor = Color.fromARGB(255, 207, 96, 74);
  //change the IP address with your host device (Python script running) IP address
  static const String serverComputerIpAddress = "192.168.0.122";
}
